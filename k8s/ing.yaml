---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: lcc
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: lcc
            port:
              number: 80
